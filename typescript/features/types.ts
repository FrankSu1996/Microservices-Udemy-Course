const today = new Date();

// can get methods and attributes with autocomplete
today.getMonth();

// creating new object, since we didn't have type annotation hovering
// person will just print out entire object
const person = {
  age: 20,
};

class Color {}

const red = new Color();
